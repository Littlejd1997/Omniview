<div class="container-fluid">
  <div class="col-md-6 col-md-offset-3 bioContainer">
    <img class="img-circle " src="https://twitter.com/<%=@user.twitterhandle%>/profile_image?size=bigger" alt=""/>
    <h3><%= @user.firstname %> <%= @user.lastname %><br/><i><small>@<%= @user.twitterhandle%></small></i></h3>
    <p><%= @user.bio %></p>
    <% unless current_user.nil? && current_user == @user %>
    <p><%= link_to 'Authorize', $fboauth.url_for_oauth_code(permissions: "manage_pages publish_pages") %> your Facebook account for native exports!</p>
        <% end %>
  </div>

</div>

<% @user.periscopes.where(completed: true).each do |periscope| %>
    <%= render partial: 'periscopes/preview', locals: {periscope: periscope} %>
    <!--<tr>-->
      <!--<td><%= link_to periscope.title, "https://www.periscope.tv/w/#{periscope.broadcast_id}" %></td>-->
      <!--<% if periscope.completed %>-->
          <!--<td>-->

          <!--</td>-->
      <% end %></tr>
<% end %>

